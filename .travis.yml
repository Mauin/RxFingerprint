language: android

android:
  components:
      - platform-tools
      - tools
      - tools-preview
      - build-tools-24.0.1
      - android-24

      # Additional components
      - extra-android-m2repository

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.gradle

script:
  ./gradlew clean build check